apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kubeconformtests.stable.example.com
spec:
  group: stable.example.com
  names:
    kind: KubeconformTest
    listKind: KubeconformTestList
    plural: kubeconformtests
    singular: kubeconformtest
  scope: Namespaced
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              replicas:
                type: integer
                minimum: 1
              image:
                type: string
