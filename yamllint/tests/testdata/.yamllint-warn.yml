---
extends: relaxed

rules:
  # err case for err.yaml
  line-length: disable
  trailing-spaces:
    level: warning
  # differ from default config, for testing config mount
  new-line-at-end-of-file: disable
