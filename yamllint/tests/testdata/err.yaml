config:
  default: true
  line-length: false

globs:
  - "**/*.{md,markdown}"

ignores:
  - "CHANGELOG.md"
  - "docs/cli/**"
  - "docs/apis/**"
  - "releases/**"
  - "This line is going to be very long so we can intentionally generate a yamllint error. We can check the output of tests on this file to ensure errors report useful context, and not just an exit code as dagger does by default."